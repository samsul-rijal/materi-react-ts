export interface Todo {
    id: number
    task: string
    complete: boolean
}


export interface User {
    id: number
    name: string
    email: string
    address: string
}

export interface Product {
    id: number
    name: string
    price: number
    stock: number
    image: string
}
